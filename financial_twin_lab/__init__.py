"""Capital Scope Financial Twin.

A quiz-based experience that builds a synthetic financial twin of the user,
observes its likely reactions to market situations, and lets a deterministic
quantitative engine build a behaviorally-coherent portfolio.

Positioning: synthetic twin, estimated profile, probabilistic forecast. Never a
perfect copy, never guaranteed returns. Educational/experimental only — not
financial advice. No LLM calls, no real orders, no future data in the MVP.
"""

__version__ = "0.1.0"
CODE_VERSION = __version__
QUIZ_VERSION = "quiz-1.0.0"
SCORING_VERSION = "scoring-1.0.0"
PROFILE_VERSION = "profile-1.0.0"
RISK_FORMULA_VERSION = "risk-1.0.0"
